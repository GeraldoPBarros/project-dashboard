'use client'
import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    align-items: center;
    width: 100%;
    height: 50px;
    border-bottom: 1px solid var(--wishfulBlue);
`;

export const ContentWrapper = styled.div`
    display: flex;
    width: 90%;
    height: 20px;
    justify-content: space-between;
`;